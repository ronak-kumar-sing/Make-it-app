// This file is no longer needed for app functionality
// Expo Router automatically handles the entry point from package.json (main: "expo-router/entry")
// The actual app layout is defined in app/_layout.tsx

import { Slot } from 'expo-router';
export default function App() {
  return <Slot />;
}
